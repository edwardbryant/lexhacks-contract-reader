import webbrowser

class Contract():

    """ Class for representing an unstructured contract document """

    def __init__(self, path, filename):
        """ Inits a Contract object 
        Args:
        path = a string of the path to the contract
        filename = a string of the contract's filename
        # encoding = possible arg: encoding?
        """
        self.path = path
        self.filename = filename

    def show_document():
        """ Opens contract in a web browser """
        # webbrowser.open(self. )




